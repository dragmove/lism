language: node_js

node_js:
  - 16

install:
  - npm install

script:
  - npm run lint
  - npm run test:coverage

before_deploy:
  - npm run build

deploy:
  provider: npm
  email: '$NPM_EMAIL'
  api_key: '$NPM_TOKEN'
  skip_cleanup: true
  on:
    branch: main
